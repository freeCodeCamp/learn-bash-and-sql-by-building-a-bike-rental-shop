/*const assert = require('assert');
const { getLastCommand } = require('./utils');

describe('You', () => {
  let lastCommand;
  before(async () => {
    lastCommand = await getLastCommand();
  });

  it('should view the echo manual', async () => {
    assert(lastCommand[0] === 'man' && lastCommand[1] === 'echo');
  });
});*/
