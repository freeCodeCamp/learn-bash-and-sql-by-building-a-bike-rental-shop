/*const assert = require('assert');
const { getScriptOutput } = require('./utils');

describe('Your script', () => {
  let scriptOutput;
  before(async () => {
    scriptOutput = await getScriptOutput('../bike-shop.sh', '1');
  });

  it('should echo the result of the correct psql command', async () => {
    assert(/Mountain/.test(scriptOutput) && /Road/.test(scriptOutput) && /BMX/.test(scriptOutput));
  });
});*/
